# ADR 007: Adapter architecture

## Decision

Define traits for metadata, decoding, fingerprints, storage, AI, thumbnails, proxies, NLE export, camera integration, workflow actions, and provenance.

## Consequences

Later implementations can vary by vendor, platform, and model without contaminating the domain model. Phase 0 deliberately supplies no fake implementations.
