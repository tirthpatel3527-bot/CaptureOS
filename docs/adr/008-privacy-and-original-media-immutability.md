# ADR 008: Privacy and original-media immutability

## Decision

Default media operations are read-only. CaptureOS must not modify, rename, delete, overwrite, or format user originals without explicit future workflows.

## Consequences

The Phase 0 fixture is synthetic and no user paths are inspected. Future destructive actions require clear confirmation design and an audit trail.
