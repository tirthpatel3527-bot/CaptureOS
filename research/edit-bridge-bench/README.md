# EditBridgeBench

`EditBridgeBench` uses generated metadata only. It measures deterministic M10 session-like
handoff rendering and output candidate matching at bounded presentation scales; it never opens a
CaptureOS catalog, customer media, preview, model, or network service.
